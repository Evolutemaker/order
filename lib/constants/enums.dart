enum TextfieldType {
  fullname,
  email,
  phone,
  country,
  city,
  address,
  postcode,
}

enum ButtonType {
  addAddress,
  selectAddress,
  nextStep,
}

enum HeaderType {
  senderDetails,
  recipientAddress,
}